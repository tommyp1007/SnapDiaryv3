<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <EditText
        android:id="@+id/editTextDescription"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter description"
        android:inputType="textMultiLine"
        android:gravity="start"
        android:padding="16dp"
        android:layout_alignParentTop="true" />

    <RatingBar
        android:id="@+id/ratingBarMood"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/editTextDescription"
        android:layout_marginTop="16dp"
        android:rating="0"
        android:numStars="5"
        android:stepSize="1"
        android:padding="16dp" />

    <LinearLayout
        android:id="@+id/linearLayoutButtons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/ratingBarMood"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        android:padding="16dp">

        <Button
            android:id="@+id/buttonCamera"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Take Picture"
            android:padding="16dp" />

        <Button
            android:id="@+id/buttonSelectImage"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:text="Select Image"
            android:padding="16dp" />
    </LinearLayout>

    <Button
        android:id="@+id/buttonRecordAudio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/linearLayoutButtons"
        android:layout_marginTop="33dp"
        android:padding="16dp"
        android:text="Record Audio" />

    <Button
        android:id="@+id/buttonStopAudio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonRecordAudio"
        android:layout_marginTop="16dp"
        android:text="Stop Recording"
        android:padding="16dp"
        android:visibility="gone" />

    <Button
        android:id="@+id/buttonPlaybackAudio"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonStopAudio"
        android:layout_marginTop="16dp"
        android:text="Playback Audio"
        android:padding="16dp"
        android:visibility="gone" />

    <Button
        android:id="@+id/buttonSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/buttonPlaybackAudio"
        android:layout_marginTop="16dp"
        android:text="Save Diary"
        android:padding="16dp" />

</RelativeLayout>
